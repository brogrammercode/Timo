# .env.example
# Copy this file to .env and replace the values with your actual secrets

# Firebase API keys or any other secrets can go here if not fully relying on firebase_options.dart
API_KEY=your_api_key_here
# Example configuration flag
IS_DEV=true
